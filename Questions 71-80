// Q71 - Read and print a matrix.

#include <stdio.h>

int main(void){
    int r,c; if(scanf("%d %d",&r,&c)!=2) return 0;
    int a[100][100];
    for(int i=0;i<r;i++) for(int j=0;j<c;j++) scanf("%d",&a[i][j]);
    for(int i=0;i<r;i++){ for(int j=0;j<c;j++){ if(j) printf(" "); printf("%d",a[i][j]); } printf("\n"); }
    return 0;
}

// --------------------------------------------------------------

// Q72 - Find the sum of all elements in a matrix.

#include <stdio.h>

int main(void){
    int r,c; if(scanf("%d %d",&r,&c)!=2) return 0;
    long long s=0,x;
    for(int i=0;i<r;i++) for(int j=0;j<c;j++){ scanf("%lld",&x); s+=x; }
    printf("%lld\n",s); return 0;
}

// --------------------------------------------------------------

// Q73 - Find the sum of each row of a matrix and store it in an array.

#include <stdio.h>

int main(void){
    int r,c; if(scanf("%d %d",&r,&c)!=2) return 0;
    long long a[200][200];
    for(int i=0;i<r;i++) for(int j=0;j<c;j++) scanf("%lld",&a[i][j]);
    for(int i=0;i<r;i++){
        long long s=0; for(int j=0;j<c;j++) s+=a[i][j];
        if(i) printf(" "); printf("%lld",s);
    }
    printf("\n"); return 0;
}

// --------------------------------------------------------------

// Q74 - Find the transpose of a matrix.

#include <stdio.h>

int main(void){
    int r,c; if(scanf("%d %d",&r,&c)!=2) return 0;
    int a[200][200];
    for(int i=0;i<r;i++) for(int j=0;j<c;j++) scanf("%d",&a[i][j]);
    for(int j=0;j<c;j++){ for(int i=0;i<r;i++){ if(i) printf(" "); printf("%d",a[i][j]); } printf("\n"); }
    return 0;
}

// --------------------------------------------------------------

// Q75 - Add two matrices.

#include <stdio.h>

int main(void){
    int r,c; if(scanf("%d %d",&r,&c)!=2) return 0;
    int A[200][200],B[200][200];
    for(int i=0;i<r;i++) for(int j=0;j<c;j++) scanf("%d",&A[i][j]);
    for(int i=0;i<r;i++) for(int j=0;j<c;j++) scanf("%d",&B[i][j]);
    for(int i=0;i<r;i++){ for(int j=0;j<c;j++){ if(j) printf(" "); printf("%d",A[i][j]+B[i][j]); } printf("\n"); }
    return 0;
}

// --------------------------------------------------------------

// Q76 - Check if a matrix is symmetric.

#include <stdio.h>

int main(void){
    int n,m; if(scanf("%d %d",&n,&m)!=2) return 0;
    int a[200][200];
    for(int i=0;i<n;i++) for(int j=0;j<m;j++) scanf("%d",&a[i][j]);
    if(n!=m){ printf("Not Symmetric\n"); return 0; }
    for(int i=0;i<n;i++) for(int j=i+1;j<n;j++) if(a[i][j]!=a[j][i]){ printf("Not Symmetric\n"); return 0; }
    printf("Symmetric\n"); return 0;
}

// --------------------------------------------------------------

// Q77 - Check if the elements on the diagonals of a matrix are distinct.

#include <stdio.h>

int main(void){
    int n; if(scanf("%d",&n)!=1) return 0;
    int a[200][200], seen[4001]={0}; // adjust domain if needed
    for(int i=0;i<n;i++) for(int j=0;j<n;j++) scanf("%d",&a[i][j]);
    for(int i=0;i<n;i++){
        int d=a[i][i];
        int ad=a[i][n-1-i];
        // Using a simple check with an array hash via offset; fallback if out of range would need map.
        int idx1=(d%4001+4001)%4001, idx2=(ad%4001+4001)%4001;
        if(seen[idx1]){ printf("Not Distinct\n"); return 0; }
        seen[idx1]=1;
        if(i!=n-1-i){
            if(seen[idx2]){ printf("Not Distinct\n"); return 0; }
            seen[idx2]=1;
        }
    }
    printf("Distinct\n"); return 0;
}

// --------------------------------------------------------------

// Q78 - Find the sum of anti diagonal elements for a square matrix.

#include <stdio.h>

int main(void){
    int n; if(scanf("%d",&n)!=1) return 0;
    long long s=0,x;
    for(int i=0;i<n;i++) for(int j=0;j<n;j++){ scanf("%lld",&x); if(j==n-1-i) s+=x; }
    printf("%lld\n",s); return 0;
}

// --------------------------------------------------------------

// Q79 - Perform diagonal traversal of a matrix (top-left to bottom-right, zigzag).

#include <stdio.h>

int main(void){
    int r,c; if(scanf("%d %d",&r,&c)!=2) return 0;
    int a[200][200];
    for(int i=0;i<r;i++) for(int j=0;j<c;j++) scanf("%d",&a[i][j]);
    int first=1;
    for(int s=0;s<=r+c-2;s++){
        if(s%2==0){ // up
            int i = (s<r? s: r-1);
            int j = s - i;
            while(i>=0 && j<c){
                if(!first) printf(" "); printf("%d",a[i][j]); first=0;
                i--; j++;
            }
        }else{ // down
            int j = (s<c? s: c-1);
            int i = s - j;
            while(j>=0 && i<r){
                if(!first) printf(" "); printf("%d",a[i][j]); first=0;
                i++; j--;
            }
        }
    }
    printf("\n"); return 0;
}

// --------------------------------------------------------------

// Q80 - Multiply two matrices.

#include <stdio.h>

int main(void){
    int r1,c1,r2,c2; if(scanf("%d %d",&r1,&c1)!=2) return 0;
    int A[200][200]; for(int i=0;i<r1;i++) for(int j=0;j<c1;j++) scanf("%d",&A[i][j]);
    if(scanf("%d %d",&r2,&c2)!=2) return 0;
    int B[200][200]; for(int i=0;i<r2;i++) for(int j=0;j<c2;j++) scanf("%d",&B[i][j]);
    if(c1!=r2){ printf("Incompatible\n"); return 0; }
    long long C[200][200]={0};
    for(int i=0;i<r1;i++) for(int j=0;j<c2;j++) for(int k=0;k<c1;k++) C[i][j]+= (long long)A[i][k]*B[k][j];
    for(int i=0;i<r1;i++){ for(int j=0;j<c2;j++){ if(j) printf(" "); printf("%lld",C[i][j]); } printf("\n"); }
    return 0;
}

// --------------------------------------------------------------
